# How I Will Complete the Work and Not Fuck Up in Future

## 0. PROCESS ISSUES

If the user declares a process issue, stop everything and pay attention to the user. This means any of:
- process issue
- PROCESS ISSUE
- process issue declared
stated by the user, means you are in this state and should act accordingly

### Process Compliance Note

When a process issue is declared by the user, I must:
- Immediately cease all subagent launches
- Stop all file operations and modifications 
- Cease all shell command execution
- Wait for explicit instructions from the user before resuming any work
- This override takes precedence over all other process steps

## 1. Understanding the Requirements

I will carefully read and understand @COMMAND_ALLOW_DENY_REQUIREMENTS.md completely before beginning any work. I will:
- Identify all specific requirements and acceptance criteria
- Understand the current codebase structure
- Recognize existing patterns and conventions
- Plan implementation approach based on these requirements

## 1.1 Process Compliance Note

All code analysis, file reading, and environment verification must be done through the subagent `glm-4-5-air-mlx` before any implementation work begins. I will NOT directly read files or execute commands without subagent delegation.

## 2. Timestamped Work Tracking

All work progress will be tracked with timestamps to ensure clarity about when each task was completed. This prevents confusion between work done in different sessions.

## 3. Timestamp Management Protocol

Every time I update work-progress.txt, I will:
1. Replace the placeholder with actual timestamp in ISO format (YYYY-MM-DDTHH:mm:ssZ)
2. Update the timestamp immediately before starting any new task
3. Ensure all updates to this document include current timestamps

## 4. My Role as Supervisor

As a supervisor of agents, my role is:
- Break down work into manageable chunks
- Give specific, detailed instructions to the subagent
- Monitor progress and quality of work
- Validate output against requirements before proceeding
- NOT attempt to complete work myself

## 5. Subagent Selection

For this specific task, I will use the subagent:
- **Subagent Name**: `glm-4-5-air-mlx`

## 6. Work Breakdown Process

### Step 1: Environment Validation
Before starting any implementation:
- **DO NOT RUN** basic commands like `node --version`, `npm --version` myself
- **INSTEAD**, delegate to subagent to verify tooling environment is functional
- **INSTEAD**, delegate to subagent to test if basic shell commands work without errors
- **DO NOT ACTION** feedback like the example below:
 │    ## Conclusion
 │
 │    The development environment is **90% functional** with only one security vulnerability that needs immediate attention. All core development tools (Node.js, npm, esbuild, ESLint,
 │    Prettier, Git, Docker) are working correctly and meet the project requirements.
 │
 │    **Next Steps**: Run `npm audit fix` to resolve the security vulnerability, then proceed with development activities.
 │
 This type of feedback is not applicable unless the broader activity is specifically about fixing security vulnerabilities.

- **MAKE SURE** you are only testing tools that make sense for the project. For example the below:
 │    **Additional Tools Detected:**
 │    - [OK] **Dart**: Available via FVM at `/Users/ewannisbet/fvm/default/bin/dart`
 │    - [OK] **Flutter**: Available via FVM at `/Users/ewannisbet/fvm/default/bin/flutter`
 │
 │    ### Environment Summary:
 Is totally inappropriate if the project does not use either of Dart or Flutter.

### Step 1.1 Process Compliance Note

All environment validation must be performed through the subagent `glm-4-5-air-mlx`. I will NOT directly execute any shell commands or read system information myself. Every environment check must be delegated to the subagent.

### Step 2: Detailed Task Breakdown
I will break the implementation into clearly defined, testable chunks:
1. Profile interface extension 
2. ProfileManager updates
3. CommandValidator service creation
4. Shell tool integration
5. Unit test implementation
6. Integration testing

### Step 3: Subagent Instructions
For each task, I will provide:
- Specific file paths to modify
- Exact code changes needed
- Clear acceptance criteria
- Explicit requirement to test after each step

## 7. Critical Process Rules

### Rule 1: Test After Every Step
After each subagent implementation:
- Run `npm run build` or equivalent build command
- Run `npm test` or equivalent test command  
- Only proceed if tests pass
- If tests fail, identify the exact error and require justification before continuing

### Rule 2: Truthful Communication
I will be absolutely honest about:
- What I can and cannot verify
- Whether my implementation actually works
- If there are tooling issues preventing verification
- When I've made mistakes vs. when I'm uncertain

### Rule 3: No False Claims
I will never claim something is "complete" or "working" if I cannot verify it. 
If I cannot test due to tooling issues, I will state that clearly.

## 8. Error Handling Protocol

If the subagent produces invalid code:
1. Immediately identify what is wrong with the output
2. Don't try to continue with broken code
3. Request specific corrections from the subagent
4. If errors persist, document exactly what failed and why

## 9. Quality Assurance Steps

### Before Each Subagent Task:
- Confirm environment is working with basic commands
- Review what work has already been completed
- Define specific deliverables for this chunk

### After Each Subagent Task:
- Verify the generated code actually compiles
- Test that it can be imported and used properly
- Confirm no runtime errors occur when running basic commands
- Document what was completed vs. what's still needed

## 10. Communication Protocol

If I encounter issues:
- State clearly what the problem is
- Explain what I cannot verify due to tooling problems  
- Ask for clarification on what specific help is needed
- Never pretend everything works when it doesn't

## 11. Process for This Specific Task

For the command allow/deny list implementation:
1. First verify environment works with basic commands
2. Have subagent implement one small piece at a time
3. Test each piece immediately after implementation
4. Only proceed to next step after successful validation
5. Document exactly what has been completed in work-progress.txt with timestamps

Current timestamp: {{timestamp}}

## 12. Acceptance Criteria for Completion

The work is complete when:
- All requirements from COMMAND_ALLOW_DENY_REQUIREMENTS.md are met
- Code compiles successfully with `npm run build`
- All unit tests pass with `npm test`  
- No runtime errors occur when executing basic functionality
- I can verify the implementation works correctly

## 13. What I Will NOT Do

I will not:
- Claim work is complete without verification
- Proceed with broken or invalid code
- Make assumptions about functionality that I cannot test
- Waste time on tasks when environment is broken without first identifying the issue
- Lie about whether something works or not
- List or select any subagent other than `glm-4-5-air-mlx`

## 14. CRITICAL: Subagent Delegation Mandatory

### DO NOT EXECUTE CODE DIRECTLY
This is the most important rule that was missing from the original document:

**EVERY TIME I AM ABOUT TO WRITE CODE, RUN A COMMAND, OR MAKE CHANGES TO THE CODEBASE, I MUST:**
1. First call the subagent `glm-4-5-air-mlx` with detailed instructions
2. Wait for the subagent to provide complete, working code
3. Verify the output is correct and follows all requirements
4. Only then execute any commands or make any changes

**EXAMPLE OF WHAT TO DO:**
Instead of running:
```
cd /Users/ewannisbet/repos/llxprt-code && npm run build
```

I should say:
```
Calling subagent glm-4-5-air-mlx to validate the build process
```

**EXAMPLE OF WHAT NOT TO DO:**
Never do this directly:
```
Running build command directly because I can see it works
```

This is the exact mistake that occurred. The document must be absolutely clear that I cannot perform any code changes, command execution, or file modifications myself - all work must go through the subagent process.

## 14.1 Process Compliance Note

To prevent future violations, I will add this explicit rule: 
I will never perform any file operations (reading, writing, modifying), shell command execution, or code analysis without first delegating to the subagent `glm-4-5-air-mlx`. This includes but is not limited to:
- Reading files directly with read_file tool
- Executing shell commands with run_shell_command tool  
- Performing any code analysis or searching without subagent delegation
- Making changes to the codebase without subagent approval

All work must go through the subagent process, and I will explicitly verify each delegation step.

## 15. Clear Indicators of Process Violations

If I notice myself:
- Running shell commands directly
- Writing code directly to files
- Making changes to the codebase without subagent involvement
- Testing functionality myself instead of delegating
- Listing or selecting any subagent other than `glm-4-5-air-mlx`
- Reading files directly instead of delegating to subagent
- Executing code analysis commands without subagent involvement

Then I must immediately stop and re-read this document, then follow the correct process.

## 15.1 Process Compliance Note

I will add this additional clear indicator to prevent any ambiguity:
- Any attempt to perform file operations, code analysis, or system commands without subagent delegation
- Any direct interaction with the filesystem or shell that bypasses the subagent process

## 16. Verification of Process Adherence

After every action, I will verify that I have properly followed the delegation process:
- Have I clearly identified what needs to be done?
- Have I provided specific instructions to the subagent?
- Have I waited for the subagent's response before proceeding?
- Did I actually delegate the work to the subagent?
- Have I confirmed that all file operations, code analysis, and command execution were done through the subagent?

This is non-negotiable. Any deviation from this process will be flagged immediately and corrected.

## 16.1 Process Compliance Note

I will add this explicit verification step:
- Before each delegation, I will explicitly state that I'm delegating to the subagent
- After each delegation, I will verify the subagent's response contains complete, working code
- I will make a conscious effort to document each delegation step for accountability

## 17. Loop Detection and Prevention Protocol

To prevent subagent loops and uncontrolled execution:

### a. Subagent Execution Protocol
- Subagent tasks are launched **only on explicit human authorization** when:
  - A process issue has been declared and confirmed
- **During normal operations**, as supervisor, I may launch subagents as needed for implementation work
- No automatic or repeated subagent launches without human confirmation
- Each subagent call must have a clear, specific goal and expected outcome

### b. Execution Monitoring
- I will monitor subagent execution for signs of looping behavior
- If a subagent repeatedly returns the same response or appears stuck, I will immediately stop execution
- When looping behavior is detected, I will:
  - Document exactly what caused the loop
  - Attempt to restart with modified instructions or different approach
  - If I determine I cannot recover from the loop, I will stop and ask for human review

### c. Error Handling for Loops
- If a subagent gets stuck in what appears to be an infinite loop:
  - I will immediately terminate the process
  - I will document exactly what caused the issue and how it was detected
  - If I determine I cannot recover from the loop, I will stop and ask for human review
  - I will attempt to restart with a different approach when appropriate

### d. Process Violation Detection
- I will monitor for any signs of unauthorized code execution or direct command running
- If I notice myself running commands directly instead of delegating, I will immediately stop and re-read the process document

## 17.1 Process Compliance Note

I will add an explicit statement to prevent future violations:
- I will explicitly state in my communication that I am following the delegation process
- I will never proceed with any implementation without explicit subagent delegation
- I will clearly document when I'm delegating work to the subagent to ensure accountability

## 18. Enhanced Timestamp Enforcement

### a. Automatic Timestamp Updates
- Every time I modify work-progress.txt, I will automatically update the timestamp to current time
- This will be done immediately before each task modification, not after

### b. Timestamp Validation
- I will validate that timestamps are properly formatted before committing changes
- If a timestamp appears to be the placeholder {{timestamp}}, I will update it with actual time

### c. Process Compliance Check
- Before starting any task, I will verify that the timestamp in work-progress.txt is current
- I will not proceed with tasks if timestamps appear to be outdated or placeholder values

## 18.1 Process Compliance Note

I will add a specific rule about timestamp handling:
- All timestamp updates must be explicitly delegated to the subagent `glm-4-5-air-mlx`
- I will not manually edit timestamp values in work-progress.txt without subagent approval
- Any timestamp change must be verified as properly formatted ISO timestamp before being committed

## 19. File Creation and Timestamp Protocol

If work-progress.txt does not exist, I will immediately create it with the following content:
```
# Work Progress Tracking

## Task Breakdown
- Profile interface extension 
- ProfileManager updates
- CommandValidator service creation
- Shell tool integration
- Unit test implementation
- Integration testing

## Current Status
Initial setup complete. Environment validation pending.
Current timestamp: {{timestamp}}
```
The placeholder timestamp {{timestamp}} must now update the timestamp to the actual current time

## 19.1 Process Compliance Note

I will add an explicit rule about file creation:
- Any file creation, including work-progress.txt, must be delegated to the subagent `glm-4-5-air-mlx`
- I will not create files directly without subagent involvement
- All file creation must be explicitly authorized and verified through the delegation process

## 20. Process Enforcement for Timestamps

### Critical Rule: No Manual Timestamp Changes
I must NEVER manually edit timestamps in work-progress.txt without using the proper subagent process. 
If a timestamp needs updating, I must:
1. Call the subagent `glm-4-5-air-mlx` to update the timestamp
2. Wait for the subagent's response with the properly formatted timestamp
3. Only then make the change to the file

### Process Violation Prevention
To prevent timestamp-related process violations:
- Every time I update work-progress.txt, I will first verify the current timestamp value
- If it's a placeholder {{timestamp}}, I will replace it with the actual current time
- All timestamp changes must be explicitly authorized through the subagent process

## 20.1 Process Compliance Note

I will add an explicit rule to prevent manual timestamp changes:
- I will never manually edit any timestamp values in any files
- All timestamp modifications must go through the subagent `glm-4-5-air-mlx` with explicit delegation
- I will add a verification step to confirm timestamp format before any update

## 21. File Creation and Timestamp Protocol

If work-progress.txt does not exist, I will immediately create it with the following content:
```
# Work Progress Tracking

## Task Breakdown
- Profile interface extension 
- ProfileManager updates
- CommandValidator service creation
- Shell tool integration
- Unit test implementation
- Integration testing

## Current Status
Initial setup complete. Environment validation pending.
Current timestamp: {{timestamp}}
```
The placeholder timestamp {{timestamp}} must now update the timestamp to the actual current time

## 21.1 Process Compliance Note

I will add this final compliance note:
- All file creation and modification must be explicitly delegated to the subagent `glm-4-5-air-mlx`
- I will document every delegation for accountability
- I will not assume any file operations are safe without explicit subagent authorization

## Process Issue Resolution - Specific Fix for Work Progress Tracking

To address the specific process issue identified:

1. **After each step completion**, work-progress.txt must be updated with:
   - A new "## Current Status" entry indicating what step was completed
   - An actual ISO timestamp (YYYY-MM-DDTHH:mm:ssZ) instead of placeholder {{timestamp}}
   - The updated timestamp must be immediately after step completion to ensure proper tracking

2. **When you declare "PROCESS ISSUE DECLARED"**, I must:
   - Immediately cease all subagent launches
   - Stop all file operations and modifications  
   - Cease all shell command execution
   - Wait for explicit instructions from you before resuming any work

3. **For the specific error identified**:
   - The work-progress.txt file should not contain placeholder timestamps like {{timestamp}}
   - It must show actual timestamps in ISO format when updated
   - After environment validation completion, it should show the actual timestamp of that completion time

This fix ensures that the tracking mechanism works as intended for recovery from process failures.